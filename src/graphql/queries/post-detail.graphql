query PostDetail($slug: ID!) {
  post(id: $slug, idType: SLUG) {
    id
    title
    content
    date
    author {
      node {
        name
      }
    }
    tags {
      nodes {
        name
      }
    }
    translations {
      language
      slug
    }
  }
}
