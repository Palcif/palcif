query Events($language: LanguageCodeFilterEnum!) {
  events(first: 50, where: { language: $language }) {
    nodes {
      id
      title
      slug
      content
      eventsFields {
        eventdate
        eventtime
        location
      }
    }
  }
}
