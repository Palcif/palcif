query PageContent($language: LanguageCodeFilterEnum!, $slug: String!) {
  pages(first: 10, where: { language: $language, name: $slug }) {
    nodes {
      id
      title
      content
      uri
      language
      translations {
        language
        slug
      }
    }
  }
}
