query EventDetail($slug: ID!) {
  event(id: $slug, idType: SLUG) {
    id
    title
    content
    featuredImage {
      node {
        sourceUrl
        altText
      }
    }
    eventsFields {
      eventdate
      eventtime
      location
    }
    translations {
      language
      slug
    }
  }
}
