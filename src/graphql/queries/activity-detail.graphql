query ActivityDetail($slug: ID!) {
  activity(id: $slug, idType: SLUG) {
    id
    title
    content
    date
    featuredImage {
      node {
        sourceUrl
        altText
      }
    }
    activityFields {
      category
      summary
    }
  }
}
