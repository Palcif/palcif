# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.local
.claude
docs
node_modules

# GraphQL codegen output — regenerate locally with `npm run codegen`
src/graphql/generated/

# Packaged WordPress plugin artifact — the plugin source lives in the folder;
# the zip is a build output uploaded to WP directly.
wordpress-plugins/*.zip